<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tatar.shoppinglist">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.manageitems.ItemsActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity
            android:name=".ui.shoppinglist.ShoppingListsActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity
            android:name=".ui.shoppinglistitem.ShoppingListItemActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".ui.shoppinglist.ShoppingListsActivity" />
        <activity
            android:name=".ui.completedshoppinglists.ShoppingListDisplayActivity"
            android:parentActivityName=".ui.main.MainActivity" />
        <activity
            android:name=".ui.completedshoppinglistitems.ItemDisplayActivity"
            android:parentActivityName=".ui.completedshoppinglists.ShoppingListDisplayActivity" />
    </application>

</manifest>